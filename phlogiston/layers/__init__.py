"""Reusable equivariant / neural building blocks.

Small, independently-testable layers composed by ``phlogiston.models``. See
``README.md`` in this directory for the component list and the equivariance
conventions every layer follows.
"""
