# Phlogiston — materials discovery framework dependencies
# Install torch first (CPU build) via:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2

# Scientific stack
numpy>=1.26,<2.2
pandas>=2.1
scipy>=1.11
scikit-learn>=1.3
tqdm>=4.66
pyyaml>=6.0

# Structural materials science
pymatgen>=2024.5.1        # crystal structures, CIF parsing, neighbor graphs
mp-api>=0.41.2            # Materials Project structural records

# ML primitives (verified on ROCm/MI350X). We use these ONLY for low-level
# equivariant math and graph containers -- models are implemented from scratch.
e3nn>=0.5                 # spherical harmonics, Clebsch-Gordan tensor products
torch-geometric>=2.6      # Data/Batch containers + pure-torch scatter (no pyg-lib)

# Tier-2 verification: ensemble universal ML interatomic potentials (uMLIPs) +
# relaxation/phonon tooling. All are PyTorch/NumPy based (ROCm-friendly). They
# depend on `torch` but only with lower bounds, so pip keeps the ROCm build
# already shipped by the base image (the Dockerfile also strips bare torch pins).
#
# Ensemble members are chosen for ROCm + our-stack compatibility:
#   * mace-torch is EXCLUDED -- hard-pins e3nn==0.4.4 vs our e3nn>=0.5.
#   * orb-models 0.7.0 is EXCLUDED -- dropped its ASE calculator + depends on
#     CUDA-only warp-lang (no ROCm GPU path).
# CHGNet + MatterSim are both ASE-ready, install conflict-free here, and give two
# independent potentials for the disagreement/confidence signal.
chgnet                   # CHGNet foundation potential (primary; MPtrj-trained)
mattersim                # MatterSim foundation potential (independent cross-check)
ase>=3.23                # relaxation driver (optimizers, cell filters), I/O
phonopy>=2.21            # finite-displacement phonons (dynamical stability)

# Optional: plotting for evaluation curves
matplotlib>=3.8
